# API Configuration
# By default, the UI connects to the production server.
# To use a local development server instead, set VITE_USE_LOCAL_SERVER=true

# Use local server for development (optional)
# Uncomment the line below to connect to http://localhost:4000 instead of production
# VITE_USE_LOCAL_SERVER=true

# Override with a custom API URL (optional)
# This takes precedence over both production and local server settings
# VITE_API_URL=https://your-custom-backend.com
